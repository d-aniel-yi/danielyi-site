---
phase: 07-tcpa-case-study-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md
autonomous: false
requirements:
  - CONT-02
  - CONT-03
  - CONT-04
tags:
  - content-outline
  - narrative-planning
  - tcpa
  - case-study

must_haves:
  truths:
    - "An approved OUTLINE.md exists that the prose plan (07-02) can consume verbatim"
    - "The section list is locked — order, titles, purposes, and word-count targets are explicit"
    - "Every open question from 07-RESEARCH.md Assumptions Log (A1–A8) has a locked answer"
    - "The embed medium (screenshot), its file path, and its sticky-panel placement are specified"
    - "The 'Try it live' CTA placements (hero / sticky panel / footer) and tab-behavior are specified"
    - "Any optional code-snippet slots are enumerated (0–2) with source-material citations"
    - "User has reviewed the outline and marked it approved before Plan 02 runs"
  artifacts:
    - path: ".planning/phases/07-tcpa-case-study-page/07-OUTLINE.md"
      provides: "Locked content outline consumed by the prose plan"
      contains: "Hero metadata, sticky-panel spec, numbered section list with word budgets, locked answers to A1–A8, code-snippet slots, external-link list, footer spec"
  key_links:
    - from: ".planning/phases/07-tcpa-case-study-page/07-OUTLINE.md"
      to: ".planning/phases/07-tcpa-case-study-page/07-02-PLAN.md"
      via: "07-02 read_first list references 07-OUTLINE.md as its content source"
      pattern: "07-OUTLINE\\.md"
---

<objective>
Produce `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md` — a locked, user-approved content outline that drives the prose plan (07-02-PLAN.md). This plan is content-only: no `page.tsx`, no code, no screenshot, no file changes outside the single OUTLINE.md.

Purpose: The outline-first workflow (per STATE.md v1.2 scoping decision, ROADMAP.md Phase 7 success criterion 5) requires the section structure to be approved BEFORE prose is written. This prevents rework and ensures user-directed content per CONT-04.

Output: `07-OUTLINE.md` — one file, reviewed and approved via the `autonomous: false` checkpoint at the end of this plan. After approval, 07-02-PLAN.md consumes it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-tcpa-case-study-page/07-RESEARCH.md
@.planning/phases/07-tcpa-case-study-page/07-VALIDATION.md

# Canonical content sources (read during Task 1)
@external/tcpa-visualizer/CONTEXT.md
@external/tcpa-visualizer/STATIC-BUILD.md

# Structural template reference (so outline weights align with the shape that will be implemented)
@apps/web/src/app/projects/mobi/page.tsx

<scope_anchor>
This plan MAY modify ONLY:
- `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md` (create)

This plan MUST NOT modify:
- `apps/web/src/app/projects/tcpa/page.tsx` (Plan 02)
- `apps/web/public/tcpa-preview.png` (Plan 02)
- `apps/web/src/app/projects/page.tsx` (Phase 8)
- `external/tcpa-visualizer/**` (Out of Scope per REQUIREMENTS.md)
- `infra/**` (Out of Scope per REQUIREMENTS.md)
- `apps/web/next.config.ts`

If you find yourself wanting to touch any of the above, STOP — that is a different plan/phase.
</scope_anchor>

<interfaces>
<!-- Structural contracts the OUTLINE must honor so Plan 02 can implement it without interpretation. -->
<!-- These come directly from apps/web/src/app/projects/mobi/page.tsx. -->

Hero block shape (from mobi lines 384–401):
```tsx
<header className="mb-16">
    <p className="font-mono text-xs text-gray-500 mb-4 tracking-widest uppercase">
        {KICKER_STRING}
    </p>
    <h1 className="font-serif text-4xl font-medium tracking-tight mb-6 text-gray-900">
        {TITLE_STRING}
    </h1>
    <a href="/tcpa/" className="inline-flex items-center gap-2 px-3 py-1.5 border border-gray-300 ...">
        <ExternalLink className="w-4 h-4" />
        Try it live
    </a>
</header>
```

STEPS entry shape (from mobi lines 10–27):
```tsx
{ id: "kebab-case-id", title: "Section Title", content: (<><p>...</p></>) }
```

Two-panel layout (from mobi lines 354–433):
- Outer: `flex flex-col lg:flex-row`, NO overflow utility.
- Left panel: `hidden lg:block lg:w-[35%] h-screen sticky top-0 border-r border-gray-200 bg-gray-50/50 relative`.
- Right panel: `w-full lg:w-[65%] h-auto lg:h-screen overflow-y-auto` wrapping `max-w-xl mx-auto px-8 py-24`.

Dark code block (from mobi line 37):
```
mt-6 p-4 bg-[#1e1e1e] text-gray-300 rounded-sm font-mono text-xs overflow-x-auto border border-gray-800
```

The OUTLINE must produce data that fits these slots without reshaping them.
</interfaces>

<locked_answers_template>
The outline MUST contain explicit locked answers for all eight Assumptions Log items from 07-RESEARCH.md. Research's pre-ratified defaults (treat as "approve-or-override"):

- **A1 — Kicker string:** `"Case Study 003"` (mobi is 002; TCPA is the next numbered deep-dive).
- **A2 — Target-tab behavior:** same-tab (no `target="_blank"`). `/tcpa/` is a full-viewport static bundle; same-tab nav reads as "open the app."
- **A3 — Layout shape:** Two-panel (mobi's 35%/65% sticky-left shape). PAGE-08 explicitly requires visual consistency with `/projects/mobi`; single-column is rejected.
- **A4 — Section ordering:** Chronological build flow — Problem → MotherDuck Dives → MotherDuck MCP → DuckDB-WASM → Closing.
- **A5 — Screenshot format:** `apps/web/public/tcpa-preview.png`, 1600×1000 PNG, ~300 KB target. Source = screenshot of `/tcpa/` Filings tab showing real data.
- **A6 — Secondary GitHub pill:** Omitted. Research verified no public TCPA repo exists (both `github.com/d-aniel-yi/tcpa` and `/tcpa-visualizer` return 404). Only the "Try it live" CTA appears.
- **A7 — "Dive" term hygiene:** "Dive" means **MotherDuck Dive** only (capitalize, prefix where ambiguous). The page itself is a "case study," not a "deep dive," in prose.
- **A8 — Outline artifact format:** `07-OUTLINE.md` as a sibling to PLAN.md in this phase directory. No `OUTLINE.md` precedent in this project, but it is the natural fit per ROADMAP.md Phase 7 plan descriptions.

The outline task MUST either accept each default or record the user's override — no silent acceptance, no skipped answers.
</locked_answers_template>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft 07-OUTLINE.md with full section spec and locked answers</name>
  <files>.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md</files>
  <action>
Create `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md`. Before writing, re-read the canonical source documents so the outline can cite concrete hooks — do NOT paraphrase from memory:
- `external/tcpa-visualizer/CONTEXT.md` (Problem framing — post-Duguid boundary, 4 statutes, federal-only RECAP scope, 5-tab Dive structure, MCP-driven workflow).
- `external/tcpa-visualizer/STATIC-BUILD.md` (DuckDB-WASM architecture flip, 29K rows, COOP/COEP, query numbers, same-React-different-data-source pattern).
- `.planning/phases/07-tcpa-case-study-page/07-RESEARCH.md` §Gray Area 4 (narrative hooks already identified per section) and §Gray Area 6 (code-snippet recommendations).

The OUTLINE.md file MUST contain — in this order — exactly these sections:

## 1. Metadata (YAML frontmatter)
```yaml
---
phase: 07-tcpa-case-study-page
artifact: outline
status: draft           # will flip to `approved` at task 2 approval
created: 2026-04-19
consumes: []            # read-only inputs are cited below
produces_for:
  - 07-02-PLAN.md       # the prose plan consumes this outline
locked_decisions:
  A1_kicker: "Case Study 003"
  A2_target_tab: "same-tab"
  A3_layout: "two-panel (mobi shape preserved)"
  A4_section_order: "chronological build flow"
  A5_screenshot: "apps/web/public/tcpa-preview.png, 1600x1000 PNG, ~300KB"
  A6_github_pill: "omitted (no public repo exists)"
  A7_dive_term: "MotherDuck Dive only; case study for the page itself"
  A8_outline_format: "this file"
---
```

## 2. Hero spec
- Kicker: `"Case Study 003"` (locked per A1; per RESEARCH.md Gray Area 5).
- Title: `"TCPA: In-Browser Analytics"` — defensible default; RESEARCH.md §Gray Area 5 notes alternates `"TCPA: Litigation Data in the Browser"` and `"TCPA Explorer: A Dashboard as a Static File"`. Capture user's choice here.
- CTA: single `"Try it live"` pill → `<a href="/tcpa/">` (NOT `<Link>`; trailing slash required per RESEARCH.md Pitfall 1, verified `apps/web/next.config.ts` line 14 `trailingSlash: true`). Same-tab per A2. Icon: lucide-react `<ExternalLink />`.
- No secondary pill per A6.

## 3. Sticky left-panel spec (lg+ only)
- Contents from top: screenshot `<img src="/tcpa-preview.png" alt="TCPA Litigation Explorer dashboard preview" />`, a one-line mono caption, and a secondary "Try it live" pill.
- Mono caption text (lock final wording here): `"In-browser DuckDB-WASM · 29K rows · No backend"` (per RESEARCH.md Code Examples, Left sticky panel variant).
- CTA: same pill style as hero; same `href="/tcpa/"`.
- Layout note: the outer flex container must NOT use any `overflow-*` utility (per STATE.md line 41 / RESEARCH.md Pitfall 3).

## 4. Section list — scrolling right panel
One row per STEPS entry with ID, title, one-sentence purpose, target word count, and source-material citation. Default ordering per A4:

| # | id (kebab) | Title | Purpose (1 sentence) | Word target | Source |
|---|-----------|-------|---------------------|-------------|--------|
| 1 | `the-problem` | "The Problem: Post-Duguid TCPA Trend Analysis" | Frame why trend-tracking consumer-protection litigation requires a new tool after the April 2021 *Facebook v. Duguid* boundary. | ~750 | CONTEXT.md lines 7–18, 100–142 |
| 2 | `motherduck-dives` | "MotherDuck Dives: Dashboards as Code" | Explain Dives (BI-as-code, 5-tab structure) and why they beat a traditional BI tool for a narrative dashboard. | ~500 | CONTEXT.md lines 137–142; motherduck.com/product/dives/ |
| 3 | `motherduck-mcp` | "MotherDuck MCP: Conversational ETL" | Show how the 3-step enrichment pipeline evolved via Claude + MCP and why schema-aware iteration changed the build loop. | ~400 | CONTEXT.md lines 121–134; motherduck.com/docs/sql-reference/mcp/ |
| 4 | `duckdb-wasm` | "DuckDB-WASM: Zero-Backend Publishing" | The marquee: why the Dive is not the final form — the `/tcpa` static bundle exists so viewers need no account. Cover the architecture flip, 29K rows, COOP/COEP, same-React-different-provider. | ~750 | STATIC-BUILD.md lines 7–17, 91, 215–220, 252–263, 423–441 |
| 5 | `closing` | "What This Stack Lets You Ship" | One-paragraph reflection: a live dashboard deployed as a static file. | ~100 | — |

Total word target: ~2,500. Ratio: 750/1,750 = ~30% problem / ~70% stack — satisfies CONT-02 and CONT-03 weight requirements.

The outline MUST note: "If user adjusts word counts, keep the problem:stack ratio within ~30/70 ± 5pp so CONT-02/CONT-03 remain satisfied."

## 5. Code-snippet slots (0–2 recommended; RESEARCH.md §Gray Area 6 proposed 3)
Lock the count and the per-snippet source here. Research recommends up to 3; keep the menu at ≤2 by default to stay closer to the narrative-first tone of this page (mobi uses 5 code blocks across 7 sections, but TCPA has fewer sections and a shorter total word count). Defaults:

1. **MotherDuck Dives section** — SQL excerpt (10–12 lines) showing classification/top-plaintiffs query. Uses the mobi dark-block pattern (`bg-[#1e1e1e] text-gray-300 ... overflow-x-auto border border-gray-800`). Syntax colors per RESEARCH.md Pattern 2. Source: `external/tcpa-visualizer/.dive-preview/src/dive.tsx` — note that this file is NOT in the read-set during planning; Plan 02 executor must read it at prose-write time.
2. **DuckDB-WASM section** — ~8 lines showing `registerFileBuffer` + `CREATE TABLE ... AS SELECT * FROM read_parquet(...)`. Source: `external/tcpa-visualizer/STATIC-BUILD.md` lines 215–220.

Omit the MCP `.mcp.json` snippet unless user requests it. Record user's final count here (0, 1, or 2).

## 6. External links (in-prose anchor tags — plain text, not hero CTAs)
Recommended per RESEARCH.md §Gray Area 6; record which are accepted:
- `https://motherduck.com/docs/key-tasks/ai-and-motherduck/dives/` — Dives section.
- `https://github.com/motherduckdb/mcp-server-motherduck` — MCP section.
- `https://github.com/duckdb/duckdb-wasm` — DuckDB-WASM section.
- `https://www.courtlistener.com/help/api/rest/` — Problem section.
- `https://www.supremecourt.gov/opinions/20pdf/19-511_p86b.pdf` — Problem section (*Facebook v. Duguid* anchor).

Inline link styling: `text-gray-900 underline underline-offset-4 decoration-gray-300 hover:decoration-gray-900` (mobi has no in-prose link precedent, so this is a small extension — record here explicitly so Plan 02 doesn't reinvent it).

## 7. End-of-page footer spec
- Horizontal divider (`mt-24 pt-8 border-t border-gray-200`) mirroring mobi's end-of-page block (page.tsx lines 417–428).
- Single `"Try it live"` pill, same style as hero, same `href="/tcpa/"`.
- Trailing `<div className="h-24" />` spacer to match mobi.

## 8. Term hygiene guardrails (enforced against Plan 02 prose)
- Use `"MotherDuck Dive"` (capitalized, two words) when referring to the product.
- Call the page a `"case study"` in prose, NOT a `"deep dive"`. Reserve `"dive"` for the MotherDuck product. (Per A7 / STATE.md line 47 / RESEARCH.md Pitfall 4.)
- When referring to the `/tcpa/` app, prefer `"the live visualizer"` or `"the /tcpa app"`; avoid `"the dive at /tcpa"` (it's a DuckDB-WASM derivative of the Dive, not the Dive itself — this distinction is a narrative beat in section 4).

## 9. Locked Answers summary (A1–A8)
Reproduce the locked answers table from the frontmatter as a human-readable table. For each, show: assumption ID, research default, and the user's decision (`accepted` or override text).

## 10. Handoff note to Plan 02
Short paragraph listing exactly what Plan 02 consumes from this outline:
- Section list (§4) → STEPS array IDs + titles.
- Screenshot spec (§3) → file path, dimensions, target size.
- CTA spec (§2, §3, §7) → three `<a href="/tcpa/">` locations + icon + tab behavior.
- Code-snippet slots (§5) → which sections get blocks and their source files.
- External links (§6) → inline link URLs + styling class string.
- Term hygiene (§8) → prose guardrails.

Write the file via the Write tool. Do NOT run any build or TypeScript commands — this plan produces a markdown document, not code.

Cite each decision against its source (RESEARCH.md §GrayArea, STATE.md line, REQUIREMENTS.md Out of Scope, or mobi page.tsx lines). Do NOT introduce new decisions beyond what research surfaced.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'A1_kicker' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'A8_outline_format' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'the-problem' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'duckdb-wasm' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'tcpa-preview.png' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md &amp;&amp; grep -q 'href="/tcpa/"' .planning/phases/07-tcpa-case-study-page/07-OUTLINE.md</automated>
  </verify>
  <done>
- `07-OUTLINE.md` exists at the phase-dir path with frontmatter containing all eight `A*` locked decisions.
- All five sections (the-problem, motherduck-dives, motherduck-mcp, duckdb-wasm, closing) appear in the section list.
- Screenshot path `apps/web/public/tcpa-preview.png` appears in §3.
- CTA href `/tcpa/` (with trailing slash) appears at least once.
- Term-hygiene guardrails (§8) and handoff note (§10) are present.
- NO files outside `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md` modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approval of OUTLINE.md before Plan 02 runs</name>
  <what-built>
A fully-drafted `07-OUTLINE.md` with:
- Locked answers to A1–A8 (kicker, tab behavior, layout, ordering, screenshot, GitHub pill, term hygiene, outline format).
- Hero/sticky-panel/footer specs that plug directly into mobi's structural slots.
- 5-entry section list with IDs, titles, purposes, word counts, and source-material citations.
- Code-snippet slot decisions (default 2).
- External-link list with inline styling class string.
- Term-hygiene guardrails ("MotherDuck Dive" not "deep dive"; page is a "case study").
- Handoff paragraph enumerating what Plan 02 consumes.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md` in the editor.
2. Review each of the eight locked answers (A1–A8). For any default you want to override, edit the file directly:
   - A1 kicker — prefer a different string? (e.g., `"Case Study 002.5"`, date-based, or something else.)
   - A2 tab behavior — prefer new tab (`target="_blank"`)?
   - A3 layout — confirm two-panel (single-column would break PAGE-08).
   - A4 section ordering — prefer a different arc (e.g., thematic, or lead with DuckDB-WASM)?
   - A5 screenshot — confirm source (dev run vs deployed), format, target size.
   - A6 GitHub pill — will a public TCPA repo be published? If yes, add a secondary pill spec.
   - A7 term hygiene — confirm "MotherDuck Dive" / "case study" discipline.
   - A8 outline format — confirm this file is the handoff artifact.
3. Review the section list word counts. Ratio must stay within ~30% problem / ~70% stack ± 5pp. Adjust counts if desired.
4. Review code-snippet count — 0, 1, or 2 blocks. The default is 2 (SQL + DuckDB-WASM init).
5. Review external-link list — remove any links you don't want shown.
6. When satisfied, flip the frontmatter `status: draft` to `status: approved` and save.
7. Respond "approved" to resume.

**Do NOT** approve if:
- Any A1–A8 answer is still showing a `TBD` or placeholder.
- Word counts no longer sum to ~2,500 or break the 30/70 ratio.
- Section IDs contain spaces/uppercase (must be kebab-case for STEPS array ids).
- The file references anything outside the phase dir (that's Plan 02's job).
  </how-to-verify>
  <resume-signal>Type "approved" (after flipping frontmatter `status: approved`) or describe requested edits</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan writes a single markdown document inside `.planning/`. No code, no runtime, no network, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `07-OUTLINE.md` contents | accept | Outline may reference architectural hooks already public (mobi page.tsx, CONTEXT.md, STATIC-BUILD.md). No secrets, no PII, no tokens. The planning dir is intended to be committed with the repo. |
| T-07-02 | Tampering | Scope creep into `page.tsx` or `infra/` | mitigate | `<scope_anchor>` in the context section + explicit `files_modified` frontmatter list restrict this plan to a single markdown file. Automated verify greps for required structure in OUTLINE.md only — build is not run. |
</threat_model>

<verification>
## Plan-level checks
- `.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md` exists and is non-empty.
- Frontmatter includes all eight `A*` locked-decision keys.
- All five section IDs (`the-problem`, `motherduck-dives`, `motherduck-mcp`, `duckdb-wasm`, `closing`) appear.
- Screenshot path `apps/web/public/tcpa-preview.png` appears.
- CTA href `/tcpa/` (with trailing slash) appears.
- Status flipped from `draft` to `approved` (Task 2 user action).
- No files outside `.planning/phases/07-tcpa-case-study-page/` have been modified (git status check at end: only `07-OUTLINE.md` appears as a new file under phase dir).
</verification>

<success_criteria>
- `07-OUTLINE.md` exists and contains all ten sections described in Task 1's action.
- All eight Assumptions Log decisions (A1–A8) are locked with user-accepted or user-overridden values.
- Task 2 checkpoint reached "approved" state.
- No other files created or modified (verified by `git status`).
- Plan 02 can begin because it now has a concrete content contract to implement.
</success_criteria>

<output>
After Task 2 resumes with "approved":
1. Create `.planning/phases/07-tcpa-case-study-page/07-01-SUMMARY.md` documenting:
   - Final locked values for A1–A8 (note any overrides from research defaults).
   - Final word-count budget per section.
   - Final code-snippet count.
   - Any user-requested additions to the external-link list.
   - Any prose guardrails beyond A7 term hygiene.
2. Commit the OUTLINE + SUMMARY together: `docs(07): lock TCPA case-study outline`.
</output>
