---
phase: 08-listing-integration-build-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/projects/page.tsx
autonomous: false
requirements:
  - PAGE-07
  - LIST-01
tags:
  - nextjs
  - projects-listing
  - static-export
  - build-validation
  - tcpa

must_haves:
  truths:
    - "Visiting `/projects` in `npm run dev` renders four `deep-dive` cards: FSBO, Mobi, Portfolio, and the new TCPA card"
    - "The TCPA card's primary link resolves to `/projects/tcpa` (via `detailsSlug: \"tcpa\"`, using ProjectCard's linkHref rule — same-tab, no target=_blank)"
    - "The TCPA card renders the existing `/tcpa-preview.png` asset as its thumbnail"
    - "`cd apps/web && npm run build` exits 0 with no new warnings or errors beyond the Phase 7 baseline"
    - "`cd apps/web && npx tsc --noEmit` exits 0"
    - "`apps/web/out/projects/index.html` exists after build and contains a link to `/projects/tcpa/`"
    - "`apps/web/out/projects/tcpa/index.html` still exists after build (no Phase 7 regression)"
    - "`apps/web/out/projects/{fsbo,mobi,portfolio}/index.html` all still exist after build (no deep-dive regressions)"
  artifacts:
    - path: "apps/web/src/app/projects/page.tsx"
      provides: "Projects listing page with TCPA entry added to the `featuredProjects` array"
      contains: "detailsSlug: \"tcpa\""
      min_lines: 215
    - path: "apps/web/out/projects/index.html"
      provides: "Static-export artifact for the listing page, containing a hyperlink to /projects/tcpa/"
      min_bytes: 5000
    - path: "apps/web/out/projects/tcpa/index.html"
      provides: "Static-export artifact for the Phase 7 TCPA case study page (regression gate)"
      min_bytes: 30000
  key_links:
    - from: "apps/web/src/app/projects/page.tsx"
      to: "apps/web/src/app/projects/tcpa/page.tsx"
      via: "detailsSlug: \"tcpa\" in the featuredProjects TCPA entry → ProjectCard resolves linkHref to `/projects/tcpa`"
      pattern: "detailsSlug:\\s*\"tcpa\""
    - from: "apps/web/src/app/projects/page.tsx"
      to: "apps/web/public/tcpa-preview.png"
      via: "image: \"/tcpa-preview.png\" in the featuredProjects TCPA entry"
      pattern: "/tcpa-preview\\.png"
    - from: "apps/web/out/projects/index.html"
      to: "apps/web/out/projects/tcpa/index.html"
      via: "Static-rendered anchor tag with href=\"/projects/tcpa/\" (or equivalent; Next adds trailing slash from trailingSlash: true)"
      pattern: "/projects/tcpa"
---

<objective>
Add a TCPA deep-dive card to the `/projects` listing (LIST-01) and validate the full v1.2 static-export build (PAGE-07). One file change (`apps/web/src/app/projects/page.tsx`), one full `next build` run, and one human-verify checkpoint on the rendered listing.

Purpose: Close out v1.2. Phase 7 shipped `/projects/tcpa` as a standalone page; without this phase, the case study is undiscoverable from the listing, and the full-site static export has not been validated with the new route present. This plan delivers both requirements.

Output:
- `apps/web/src/app/projects/page.tsx` modified in place — one new entry appended to `featuredProjects: ProjectItem[]`
- `apps/web/out/projects/index.html` containing a rendered anchor to `/projects/tcpa/`
- `apps/web/out/projects/tcpa/index.html` still present (Phase 7 regression gate)
- `apps/web/out/projects/{fsbo,mobi,portfolio}/index.html` all still present (no deep-dive regressions)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 7 shipped context — the TCPA page this card links to
@.planning/phases/07-tcpa-case-study-page/07-02-SUMMARY.md
@.planning/phases/07-tcpa-case-study-page/07-OUTLINE.md

# The file this plan modifies — contains the three existing deep-dive cards to follow as a pattern
@apps/web/src/app/projects/page.tsx

# The component contract the entry must satisfy (ProjectItem type)
@apps/web/src/components/projects/ProjectCard.tsx

# The page the card links to (existence check only — DO NOT MODIFY)
@apps/web/src/app/projects/tcpa/page.tsx

# Build configuration (informs static-export expectations)
@apps/web/next.config.ts

<scope_anchor>
This plan MAY create/modify ONLY:
- `apps/web/src/app/projects/page.tsx` — add exactly ONE new entry to the `featuredProjects` array

This plan MUST NOT modify:
- `apps/web/src/app/projects/tcpa/page.tsx` — Phase 7 surface is frozen
- `apps/web/src/components/projects/ProjectCard.tsx` — no new card variants/props needed
- `apps/web/src/app/projects/mobi/page.tsx`, `fsbo/page.tsx`, `portfolio/page.tsx` — unaffected
- `apps/web/public/tcpa-preview.png` — reuse the Phase 7 asset as-is
- `apps/web/next.config.ts` — build config unchanged
- `infra/**` — no deployment changes this phase
- `external/**` — visualizer source is out of scope

This plan MUST NOT:
- Add new props/fields to the `ProjectItem` interface
- Introduce a new `variant` on `ProjectCard`
- Change the order of existing `featuredProjects` entries (append only)
- Change anything in the `standardProjects` array
- Modify the listing page's header, grain overlay, or section structure
- Delete or re-cast existing entries
</scope_anchor>

<interfaces>
<!-- ProjectItem contract from apps/web/src/components/projects/ProjectCard.tsx lines 8-24. -->
<!-- The executor MUST populate the TCPA entry against this exact shape. -->

```typescript
export interface ProjectItem {
  title: string;
  href: string;                       // required by type; when detailsSlug is set, ProjectCard uses `/projects/${detailsSlug}` as the effective link
  excerpt: string;
  image: string;                      // path relative to public/ (e.g. "/tcpa-preview.png")
  tags?: string[];
  featured?: boolean;                 // true → rendered via variant="deep-dive" in the featured section loop
  accentColor?: string;               // hex string; used for border/tag/hover tinting in deep-dive variant
  detailsSlug?: string;               // when set, ProjectCard routes to /projects/${detailsSlug} and renders as internal (same-tab, no target=_blank)
  github?: string;
  basescan?: string;
  techDetails?: {
    stack: string[];
    architecture: string;
    highlights: string[];             // bulleted list shown in the expanded details panel
  };
}
```

<!-- How the listing page consumes this (apps/web/src/app/projects/page.tsx line 187): -->
<!--   {featuredProjects.map((project) => ( -->
<!--     <ProjectCard key={project.title} item={project} variant="deep-dive" /> -->
<!--   ))} -->

<!-- ProjectCard linkHref resolution (ProjectCard.tsx lines 34-36): -->
<!--   const linkHref = item.detailsSlug ? `/projects/${item.detailsSlug}` : item.href; -->
<!--   const isExternal = !item.detailsSlug; -->
<!-- → Setting detailsSlug: "tcpa" makes the card link to /projects/tcpa (internal, same tab). -->
<!-- → The top-level `href` field still satisfies the type; convention from existing cards is to set it to the same internal path. -->
</interfaces>

<pattern_reference>
**Closest analog — the Mobi entry (featuredProjects[1], lines 28–48 of page.tsx):**

```typescript
{
  title: "Mobi Measure",
  href: "https://mobi-measure.com",
  excerpt: "A high-performance image metrology suite for semiconductor analysis. ...",
  image: "/mobi-measure.png",
  tags: ["startup", "SaaS"],
  featured: true,
  accentColor: "#D9A16A",
  detailsSlug: "mobi",
  techDetails: {
    stack: ["React", "TypeScript", "Next.js", "Node.js", "PostgreSQL", "AWS"],
    architecture: "Client-side image processing with server-side persistence. ...",
    highlights: [
      "Sub-pixel measurement accuracy",
      "Support for 100+ megapixel images",
      "Real-time collaboration features",
      "Industry-standard export formats",
    ],
  },
},
```

**Mirror this shape for TCPA exactly.** Same field order, same trailing comma on the last object property, same 4-item highlights length.
</pattern_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append TCPA entry to featuredProjects array</name>
  <files>apps/web/src/app/projects/page.tsx</files>
  <action>
Open `apps/web/src/app/projects/page.tsx`. Locate the `featuredProjects: ProjectItem[]` array (starts line 7, ends around line 69 — the three existing entries are FSBO, Mobi Measure, Daniel Yi Portfolio Website).

**Append ONE new entry as the fourth element of `featuredProjects`** (after the Portfolio entry, before the closing `];`). Do NOT reorder existing entries. Do NOT modify `standardProjects`. Do NOT touch any other file.

Use these exact values (values are derived from Phase 7's shipped content per the 07-02-SUMMARY.md and 07-OUTLINE.md):

```typescript
{
  title: "TCPA Litigation Explorer",
  href: "/projects/tcpa",
  excerpt: "A case study in zero-backend analytics. After Facebook v. Duguid redrew the ATDS boundary, tracking post-2021 TCPA litigation trends required a new tool. Built on MotherDuck Dives and an MCP-driven build loop, then published as a static site running DuckDB-WASM in the browser — 29K rows, no backend, no account required.",
  image: "/tcpa-preview.png",
  tags: ["DuckDB-WASM", "MotherDuck", "Next.js"],
  featured: true,
  accentColor: "#f59e0b",
  detailsSlug: "tcpa",
  techDetails: {
    stack: ["DuckDB-WASM", "MotherDuck", "MCP", "Next.js", "Parquet"],
    architecture: "Dual-execution React dashboard: the same component tree runs against MotherDuck (as a Dive) or against DuckDB-WASM in the browser, only the query provider differs. Static bundle served from S3/CloudFront with COOP/COEP enabled.",
    highlights: [
      "In-browser DuckDB-WASM (29K rows, zero backend)",
      "MotherDuck Dives dashboard-as-code",
      "MCP-driven conversational ETL pipeline",
      "Post-Duguid TCPA trend analysis across 4 statutes",
    ],
  },
},
```

**Why these values:**
- `title` — matches the subject of the Phase 7 case study (page title on `/projects/tcpa` itself is stylized as "TCPA: In-Browser Analytics"; the card uses the plainer product-name form to match the FSBO / Mobi / Portfolio naming convention, which all name the subject rather than the case-study's own title).
- `href: "/projects/tcpa"` — satisfies the required `ProjectItem.href` field; ProjectCard's `detailsSlug` branch takes precedence for routing, but setting `href` to the same internal path keeps the object valid and self-consistent.
- `excerpt` — ~60 words covering the three narrative beats Phase 7 shipped: post-Duguid trend analysis (problem), MotherDuck Dives + MCP (build), DuckDB-WASM static publish (marquee). Follows the "Flat Fee RE"-style information density (excerpt is ~55–65 words).
- `image: "/tcpa-preview.png"` — reuses the Phase 7 asset already in `apps/web/public/tcpa-preview.png` (288,337 bytes, 1600×1000). No new asset is created.
- `tags: ["DuckDB-WASM", "MotherDuck", "Next.js"]` — 3 tags match the existing deep-dive pattern (FSBO has 3, Mobi has 2, Portfolio has 0; 3 is within the established range and conveys the stack at a glance).
- `featured: true` — required for `variant="deep-dive"` rendering at line 187 of the page.
- `accentColor: "#f59e0b"` (amber-500) — complements the existing trio (FSBO `#3b82f6` blue, Mobi `#D9A16A` warm tan, Portfolio `#8b5cf6` violet) with a distinct warm gold that evokes DuckDB's brand identity without clashing.
- `detailsSlug: "tcpa"` — drives ProjectCard to link internally to `/projects/tcpa` (same tab, no `target="_blank"`). This is the primary integration contract for LIST-01.
- `techDetails.stack` — 5 items (matches FSBO's 5 and Mobi's 6; keeps the tech-chip row to one line at common widths).
- `techDetails.architecture` — one sentence capturing the dual-execution hinge (the architectural narrative of §4 duckdb-wasm in the Phase 7 outline).
- `techDetails.highlights` — exactly 4 items (matches FSBO's 4, Mobi's 4, Portfolio's 4 — established pattern).

**Do not add any fields not shown above** (no `github`, no `basescan` — there is no public repo per Phase 7 outline A6).

Preserve formatting: 2-space indentation, trailing comma after the final object property, trailing comma after the entry itself (matching the existing style of the other three `featuredProjects` entries).
  </action>
  <verify>
<automated>cd apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>
- The TCPA entry is the fourth element of `featuredProjects` (FSBO, Mobi, Portfolio, TCPA — in that order).
- `npx tsc --noEmit` exits 0.
- `grep -c 'detailsSlug:' apps/web/src/app/projects/page.tsx` returns 4 (was 3; FSBO + Mobi + Portfolio + TCPA).
- `grep 'detailsSlug: "tcpa"' apps/web/src/app/projects/page.tsx` matches.
- `grep 'image: "/tcpa-preview.png"' apps/web/src/app/projects/page.tsx` matches.
- `grep 'accentColor: "#f59e0b"' apps/web/src/app/projects/page.tsx` matches.
- No other files changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full static-export build + no-regression validation</name>
  <files>apps/web/out/**, apps/web/.next/** (build outputs only — no source files modified)</files>
  <action>
Run a clean full static export and verify all expected routes are present in `apps/web/out/`. This validates both PAGE-07 (build passes with v1.2 changes) and LIST-01 (the listing renders the TCPA link).

**Commands (run in order):**

1. Clean any stale build output so assertions below test a fresh build:
   ```
   cd apps/web && rm -rf out .next
   ```

2. Run the full static export:
   ```
   cd apps/web && npm run build
   ```
   MUST exit 0. Capture the route table from the build output (Next prints a per-route size summary at the end) — expect to see `/projects`, `/projects/tcpa`, `/projects/fsbo`, `/projects/mobi`, `/projects/portfolio` all listed.

3. Verify the listing export artifact exists and contains a link to the TCPA page:
   ```
   test -f apps/web/out/projects/index.html
   grep -q '/projects/tcpa' apps/web/out/projects/index.html
   ```
   (The second grep covers both `/projects/tcpa` and `/projects/tcpa/` — trailingSlash: true means the rendered anchor will end with a slash, but the substring match tolerates either form.)

4. Verify Phase 7's TCPA page is still exported (no regression from the listing edit):
   ```
   test -f apps/web/out/projects/tcpa/index.html
   ```

5. Verify the three pre-existing deep-dive pages are still exported (no regression):
   ```
   test -f apps/web/out/projects/fsbo/index.html
   test -f apps/web/out/projects/mobi/index.html
   test -f apps/web/out/projects/portfolio/index.html
   ```

6. Verify the TCPA preview asset is still bundled (reused by Phase 7 page and the new listing card):
   ```
   test -f apps/web/out/tcpa-preview.png
   ```

7. Verify the listing's top-level export exists (landing page regression gate):
   ```
   test -f apps/web/out/index.html
   ```

**If ANY check fails:** stop, report the failure, do not proceed to Task 3. Do not attempt auto-fixes that would require editing files outside the scope_anchor — surface the failure to the user.

**Build warnings are acceptable IF** they are the same warnings Phase 7's Plan 02 produced (see 07-02-SUMMARY.md "Verification Snapshot" — build exited 0 with route `/projects/tcpa` at 8.58 kB / 128 kB first-load JS, no new warnings noted). **NEW** warnings or errors specific to the listing edit are a failure.
  </action>
  <verify>
<automated>cd apps/web && rm -rf out .next && npm run build && test -f out/projects/index.html && grep -q '/projects/tcpa' out/projects/index.html && test -f out/projects/tcpa/index.html && test -f out/projects/fsbo/index.html && test -f out/projects/mobi/index.html && test -f out/projects/portfolio/index.html && test -f out/tcpa-preview.png && test -f out/index.html</automated>
  </verify>
  <done>
- `npm run build` exits 0 from a clean `.next`/`out`.
- The Next route summary lists `/projects`, `/projects/tcpa`, `/projects/fsbo`, `/projects/mobi`, `/projects/portfolio`.
- `apps/web/out/projects/index.html` exists and contains the substring `/projects/tcpa`.
- `apps/web/out/projects/tcpa/index.html` exists.
- `apps/web/out/projects/{fsbo,mobi,portfolio}/index.html` all exist.
- `apps/web/out/tcpa-preview.png` exists.
- `apps/web/out/index.html` exists.
- No new build warnings attributable to the listing edit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual confirmation of listing integration</name>
  <files>(no file writes — human-verify pause for visual review of the dev server output)</files>
  <action>
Pause execution and wait for the user to visually verify the `/projects` listing in a browser. See `<what-built>` for the shipped feature summary and `<how-to-verify>` for the exact test script. Wait for the resume signal in `<resume-signal>` before concluding the plan.
  </action>
  <what-built>
Added a fourth "Engineering Deep Dives" card to `/projects` for the TCPA Litigation Explorer, reusing Phase 7's 281 KB `tcpa-preview.png` thumbnail, linking internally to `/projects/tcpa`, with a ~60-word excerpt, 3 tags (DuckDB-WASM / MotherDuck / Next.js), amber accent (`#f59e0b`), and a 4-item tech highlights list in the expanded details panel. The full static export built cleanly (Task 2 all checks green); all four deep-dive pages and the landing page are present in `apps/web/out/`.
  </what-built>
  <how-to-verify>
1. Start the dev server:
   ```
   cd apps/web && npm run dev
   ```
2. Open `http://localhost:3000/projects`.
3. Scroll to the **Engineering Deep Dives** section. Confirm you see **four** cards in this order: Flat Fee RE, Mobi Measure, Daniel Yi Portfolio Website, **TCPA Litigation Explorer** (new).
4. On the new TCPA card:
   - Confirm the thumbnail shows the TCPA visualizer screenshot (not a broken image).
   - Confirm the three tags render: `DuckDB-WASM`, `MotherDuck`, `Next.js`.
   - Confirm the title reads "TCPA Litigation Explorer".
   - Confirm the excerpt reads naturally (no truncation artifact, no encoding issue).
   - Hover the card — the amber accent border should tint on hover (same transition as the other three deep-dive cards).
5. Click the TCPA card. Confirm it navigates to `/projects/tcpa` **in the same tab** (no `target="_blank"` behavior). The Phase 7 case study page should load.
6. Click your browser's back button. Confirm you return to `/projects` with the listing intact.
7. (Optional spot-check) Click each of the other three deep-dive cards (FSBO, Mobi, Portfolio) briefly to confirm none regressed. Return to `/projects` after each.
8. (Optional spot-check for the static export, not dev) From the repo root, you can serve `apps/web/out/` via any static server (e.g. `npx serve apps/web/out`) and repeat steps 2–6 against the exported HTML to confirm production-shape behavior. This is optional — Task 2's automated checks already verified the export contents.

**Expected result:** The four-card grid reads as a natural family; the new TCPA card matches the visual weight of the other three; the click-through to `/projects/tcpa` is instant and same-tab; no console errors in DevTools.
  </how-to-verify>
  <verify>
<automated>MISSING — visual verification only; automation covered by Task 2's full build + grep check which already asserts `/projects/tcpa` is present in the rendered `out/projects/index.html`</automated>
  </verify>
  <done>
- User types "approved" (or equivalent) confirming the four-card grid renders correctly and the TCPA card click-through navigates to `/projects/tcpa` in the same tab.
- No console errors reported during the browser check.
- No regressions observed on the three pre-existing deep-dive cards (FSBO, Mobi, Portfolio).
  </done>
  <resume-signal>
Type "approved" if the listing + click-through behavior matches expectations.

If anything is off, describe the issue (e.g. "excerpt is too long / accentColor clashes / thumbnail is pixelated / card order wrong / click opened in new tab / any deep-dive regressed"). The plan may then revise Task 1 values and re-run Task 2.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan is a static-site listing edit. No user input is accepted, no network requests are made at runtime (only a static anchor), no auth surface exists, no secrets are read. The output is a pre-rendered HTML file with a hyperlink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T (Tampering) | `apps/web/public/tcpa-preview.png` reused as listing thumbnail | accept | The asset shipped in Phase 7 with user approval; this plan does not modify it. Any tampering at rest is an infra/S3 concern, not a Phase 8 concern. |
| T-08-02 | I (Information Disclosure) | New card excerpt prose | accept | Excerpt text is copied from publicly-shipped Phase 7 narrative (already live at `/projects/tcpa`). No new information is disclosed beyond what Phase 7 already published. |
| T-08-03 | D (Denial of Service) | Static export regression breaking existing routes | mitigate | Task 2 automated checks verify `out/projects/{fsbo,mobi,portfolio,tcpa}/index.html` and `out/index.html` all exist after build. Human-verify Task 3 spot-checks the three pre-existing deep-dive cards still click through. If any regresses, the build check fails fast. |

Low overall risk — this is a presentation-layer edit that touches a single file, ships no new executable code paths, and adds a single internal anchor. The primary failure mode is a build or regression error, which Task 2's gated automation detects before Task 3 human verification.
</threat_model>

<verification>
**Automated (must all pass before Task 3):**

- `cd apps/web && npx tsc --noEmit` — exit 0
- `cd apps/web && rm -rf out .next && npm run build` — exit 0
- `grep -c 'detailsSlug:' apps/web/src/app/projects/page.tsx` — returns `4` (was 3)
- `grep 'detailsSlug: "tcpa"' apps/web/src/app/projects/page.tsx` — matches
- `grep 'image: "/tcpa-preview.png"' apps/web/src/app/projects/page.tsx` — matches
- `grep 'accentColor: "#f59e0b"' apps/web/src/app/projects/page.tsx` — matches
- `grep 'title: "TCPA Litigation Explorer"' apps/web/src/app/projects/page.tsx` — matches
- `test -f apps/web/out/projects/index.html` — OK
- `grep -q '/projects/tcpa' apps/web/out/projects/index.html` — OK (rendered listing has anchor to TCPA page)
- `test -f apps/web/out/projects/tcpa/index.html` — OK (Phase 7 page still exports)
- `test -f apps/web/out/projects/fsbo/index.html` — OK (no regression)
- `test -f apps/web/out/projects/mobi/index.html` — OK (no regression)
- `test -f apps/web/out/projects/portfolio/index.html` — OK (no regression)
- `test -f apps/web/out/index.html` — OK (landing page regression gate)
- `test -f apps/web/out/tcpa-preview.png` — OK (asset still bundled)
- No change to `apps/web/src/app/projects/tcpa/page.tsx` (Phase 7 frozen): `git diff --name-only apps/web/src/app/projects/tcpa/page.tsx` — empty
- No change to `apps/web/src/components/projects/ProjectCard.tsx`: `git diff --name-only apps/web/src/components/projects/ProjectCard.tsx` — empty

**Human-verified (Task 3):**

- Four deep-dive cards render in correct order (FSBO, Mobi, Portfolio, TCPA)
- TCPA card thumbnail, title, excerpt, tags, and accent all render cleanly
- Click on TCPA card navigates to `/projects/tcpa` in the same tab
- The three pre-existing deep-dive click-throughs still work
- No console errors
</verification>

<success_criteria>
PAGE-07 (static-export build passes):
- `npm run build` exits 0 after the listing edit
- All expected routes export to `apps/web/out/` including the three pre-existing deep-dives and the Phase 7 TCPA page

LIST-01 (TCPA card on listing):
- A TCPA deep-dive card appears on `/projects` in both dev and the static export
- The card links to `/projects/tcpa` (internal, same tab, via `detailsSlug: "tcpa"`)
- The rendered `out/projects/index.html` contains the anchor target `/projects/tcpa`

No regressions:
- Phase 7 page `/projects/tcpa` is untouched and still builds
- The three pre-existing deep-dive pages (FSBO, Mobi, Portfolio) and the landing page still build
- No new TypeScript errors; no new build warnings attributable to this edit
</success_criteria>

<output>
After completion, create `.planning/phases/08-listing-integration-build-validation/08-01-SUMMARY.md` following the `summary.md` template in `<execution_context>`. Include:

- Frontmatter: `phase`, `plan`, `subsystem: ui`, `tags`, `requires` (cites Phase 7's 07-02 for shipped TCPA page + reused screenshot asset), `provides` (TCPA card on listing; validated v1.2 static export), `affects` (closes v1.2 milestone), `tech-stack.patterns`, `key-files.modified` (just `apps/web/src/app/projects/page.tsx`), `requirements-completed` (`PAGE-07`, `LIST-01`), `duration`, `completed`.
- Shipped values table for the TCPA card entry (contracted vs actual — title, href, excerpt length, image, tags, accentColor, detailsSlug, techDetails.stack count, highlights count).
- Build verification snapshot (tsc exit code, build exit code, route table summary, all `test -f` checks, the `/projects/tcpa` grep in the rendered listing).
- Any minor deviations from the contracted Task 1 values (e.g., if an excerpt word was adjusted during human-verify revision — note it here).
- Self-check: FOUND assertions for `apps/web/out/projects/index.html`, `apps/web/out/projects/tcpa/index.html`, and the three pre-existing deep-dive HTML artifacts.

This SUMMARY closes Phase 8 and, because Phase 8 is the last phase of v1.2, closes milestone v1.2. Note that fact in the final section so the milestone-complete workflow has clear signal.
</output>
