---
phase: 02-fix-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/shared/TargetSection.tsx
  - apps/web/src/components/halborn/TargetSection.tsx
  - apps/web/src/components/softstack/TargetSection.tsx
autonomous: true

must_haves:
  truths:
    - "TargetSection component renders target information with alternating layout"
    - "Halborn page imports TargetSection without module errors"
    - "Softstack page imports TargetSection without module errors"
  artifacts:
    - path: "apps/web/src/components/shared/TargetSection.tsx"
      provides: "Shared target section component for case study pages"
      exports: ["TargetSection", "TargetProps"]
      min_lines: 40
    - path: "apps/web/src/components/halborn/TargetSection.tsx"
      provides: "Re-export shared TargetSection"
      exports: ["TargetSection"]
      min_lines: 1
    - path: "apps/web/src/components/softstack/TargetSection.tsx"
      provides: "Re-export shared TargetSection"
      exports: ["TargetSection"]
      min_lines: 1
  key_links:
    - from: "apps/web/src/app/halborn/page.tsx"
      to: "apps/web/src/components/halborn/TargetSection.tsx"
      via: "import"
      pattern: "import.*TargetSection.*from.*halborn/TargetSection"
    - from: "apps/web/src/app/softstack/page.tsx"
      to: "apps/web/src/components/softstack/TargetSection.tsx"
      via: "import"
      pattern: "import.*TargetSection.*from.*softstack/TargetSection"
    - from: "apps/web/src/components/halborn/TargetSection.tsx"
      to: "apps/web/src/components/shared/TargetSection.tsx"
      via: "re-export"
      pattern: "export.*from.*shared/TargetSection"
---

<objective>
Implement shared TargetSection component to unblock halborn and softstack pages.

Purpose: Both halborn and softstack pages use identical TargetSection implementations. Per decision diag-004, abstract to a shared component to avoid duplication. The empty stubs are causing module export errors that block the build.

Output: Shared TargetSection component with type interface, plus re-export wrappers in halborn and softstack directories to maintain existing import paths.
</objective>

<execution_context>
@C:\Users\Daniel\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Daniel\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-pages/02-RESEARCH.md
@apps/web/src/app/halborn/page.tsx
@apps/web/src/app/softstack/page.tsx

Phase 1 diagnostics identified halborn/TargetSection and softstack/TargetSection as empty files. Research lines 65-126 confirm both pages use identical implementations. Decision diag-004 mandates abstracting to shared component.
</context>

<tasks>

<task type="auto">
  <name>Create shared TargetSection component</name>
  <files>apps/web/src/components/shared/TargetSection.tsx</files>
  <action>
Create shared TargetSection component following the usage pattern in halborn/page.tsx and softstack/page.tsx (lines 111-162 in halborn per research).

**Create directory first:**
```bash
mkdir -p apps/web/src/components/shared
```

**Required Exports:**

1. **TargetProps interface** (lines 100-113 in research):
```typescript
export interface TargetProps {
  number: number;
  title: string;
  subtitle: string;
  logo: string;
  rationale: string;
  approach: string;
  value: string;
  bonus?: string;
  isEven: boolean;
}
```

2. **TargetSection component**:
```typescript
export function TargetSection(props: TargetProps)
```

**Implementation Requirements:**
- Mark as client component with "use client" directive
- Accept all TargetProps using destructuring
- Use isEven prop to alternate left-right layout (even = image left, odd = image right)
- Add "scroll-section" class to enable scroll tracking (used by halborn/softstack pages)
- Display:
  - Logo image (props.logo)
  - Number badge (props.number)
  - Title and subtitle
  - Rationale, approach, and value sections
  - Optional bonus section if props.bonus exists
- Use Tailwind v4 for styling
- Use CSS custom properties for theming (--op-black, --op-accent, --op-white)
- Match design system from halborn/softstack pages

Reference halborn/page.tsx lines 156-162 for exact usage pattern with spread operator.
  </action>
  <verify>
1. Shared directory exists: `ls apps/web/src/components/shared`
2. File is non-zero size
3. TypeScript compiles: `cd apps/web && npx tsc --noEmit src/components/shared/TargetSection.tsx`
4. Both exports exist: `grep -E "export (interface|function)" apps/web/src/components/shared/TargetSection.tsx`
  </verify>
  <done>
- TargetProps interface exported with all required fields
- TargetSection component exported
- Component accepts spread props via TargetProps interface
- Component uses isEven for alternating layout
- Component includes scroll-section class
- File marked as client component
- No TypeScript compilation errors
  </done>
</task>

<task type="auto">
  <name>Create re-export wrappers for halborn and softstack</name>
  <files>apps/web/src/components/halborn/TargetSection.tsx, apps/web/src/components/softstack/TargetSection.tsx</files>
  <action>
Replace empty stub files with re-exports to maintain existing import paths in halborn/page.tsx and softstack/page.tsx.

**For halborn/TargetSection.tsx:**
```typescript
export { TargetSection, type TargetProps } from "../shared/TargetSection";
```

**For softstack/TargetSection.tsx:**
```typescript
export { TargetSection, type TargetProps } from "../shared/TargetSection";
```

This maintains backward compatibility with existing imports while implementing the shared component per decision diag-004.
  </action>
  <verify>
1. Both files are non-zero size
2. Both files re-export TargetSection: `grep "export.*TargetSection.*from.*shared" apps/web/src/components/halborn/TargetSection.tsx apps/web/src/components/softstack/TargetSection.tsx`
3. TypeScript compiles both re-exports without errors
  </verify>
  <done>
- halborn/TargetSection.tsx re-exports shared component
- softstack/TargetSection.tsx re-exports shared component
- Both pages can import TargetSection from their original paths
- No module errors when importing
  </done>
</task>

</tasks>

<verification>
1. Run `cd apps/web && npx tsc --noEmit src/components/shared/TargetSection.tsx` - should pass
2. Verify shared exports: `grep -E "export (interface TargetProps|function TargetSection)" apps/web/src/components/shared/TargetSection.tsx`
3. Verify re-exports: `grep "export.*from.*shared" apps/web/src/components/halborn/TargetSection.tsx apps/web/src/components/softstack/TargetSection.tsx`
4. Verify all files non-empty: `wc -l apps/web/src/components/shared/TargetSection.tsx apps/web/src/components/halborn/TargetSection.tsx apps/web/src/components/softstack/TargetSection.tsx`
</verification>

<success_criteria>
- Shared TargetSection component implemented with TargetProps interface
- Component handles isEven prop for alternating layouts
- halborn/TargetSection.tsx re-exports shared component
- softstack/TargetSection.tsx re-exports shared component
- Both pages can import TargetSection without module errors
- All files pass TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix-pages/02-03-SUMMARY.md`
</output>
