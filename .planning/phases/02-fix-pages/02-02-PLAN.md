---
phase: 02-fix-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/projects/ProjectCard.tsx
autonomous: true

must_haves:
  truths:
    - "ProjectCard component renders project cards with title, excerpt, image, and tags"
    - "ProjectItem type is exported for use in pages"
    - "Projects page imports ProjectCard without module errors"
  artifacts:
    - path: "apps/web/src/components/projects/ProjectCard.tsx"
      provides: "Project card component with variant support"
      exports: ["ProjectCard", "ProjectItem"]
      min_lines: 50
  key_links:
    - from: "apps/web/src/app/projects/page.tsx"
      to: "apps/web/src/components/projects/ProjectCard.tsx"
      via: "import and usage"
      pattern: "import.*ProjectCard.*from.*ProjectCard"
---

<objective>
Implement ProjectCard component to unblock /projects listing page.

Purpose: The projects page displays 12 project cards using ProjectCard component with different variants. The empty stub is causing module export errors that block the build.

Output: Working ProjectCard component with TypeScript interface that renders project cards with optional deep-dive variant, supporting both internal and external links.
</objective>

<execution_context>
@C:\Users\Daniel\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Daniel\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-pages/02-RESEARCH.md
@apps/web/src/app/projects/page.tsx

Phase 1 diagnostics identified this as one of 4 critical empty files blocking the build. Research lines 18-61 document exact usage pattern with 12 project objects.
</context>

<tasks>

<task type="auto">
  <name>Implement ProjectCard component and ProjectItem type</name>
  <files>apps/web/src/components/projects/ProjectCard.tsx</files>
  <action>
Create ProjectCard component following the usage pattern in projects/page.tsx (lines 7-124 per research).

**Required Exports:**

1. **ProjectItem interface** (lines 27-43 in research):
```typescript
export interface ProjectItem {
  title: string;
  href: string;
  excerpt: string;
  image: string;
  tags: string[];
  featured?: boolean;
  accentColor?: string;
  detailsSlug?: string;
  techDetails?: {
    stack: string[];
    architecture: string;
    highlights: string[];
  };
}
```

2. **ProjectCard component** (lines 47-48 usage pattern):
```typescript
interface ProjectCardProps {
  item: ProjectItem;
  variant?: "deep-dive" | "standard";
}

export function ProjectCard({ item, variant = "standard" }: ProjectCardProps)
```

**Implementation Requirements:**
- Mark as client component with "use client" directive (page uses motion animations)
- Import Next.js Link and Image for routing and images
- Import motion from framer-motion for animations
- Determine href: if item.detailsSlug exists, use `/projects/${item.detailsSlug}`, otherwise use item.href
- Render card with:
  - Image (item.image)
  - Title (item.title)
  - Excerpt (item.excerpt)
  - Tags (item.tags array)
  - Optional accent color styling (item.accentColor)
- Support variant prop to adjust layout (deep-dive variant has different grid styling)
- Wrap in motion.div for animations (initial={{ opacity: 0 }}, animate={{ opacity: 1 }})
- Use Tailwind v4 classes for styling
- Make entire card clickable as Link

Reference projects/page.tsx for exact usage context. Match design system patterns used elsewhere in the codebase.
  </action>
  <verify>
1. File is non-zero size
2. TypeScript compiles: `cd apps/web && npx tsc --noEmit src/components/projects/ProjectCard.tsx`
3. Both exports exist: `grep -E "export (interface|function|const)" apps/web/src/components/projects/ProjectCard.tsx`
4. "use client" directive is present
  </verify>
  <done>
- ProjectItem interface exported with all required fields
- ProjectCard component exported
- Component accepts item and optional variant props
- Component renders card with image, title, excerpt, tags
- Component uses Next.js Link for navigation
- Component uses Framer Motion for animations
- File marked as client component
- No TypeScript compilation errors
  </done>
</task>

</tasks>

<verification>
1. Run `cd apps/web && npx tsc --noEmit src/components/projects/ProjectCard.tsx` - should pass
2. Verify exports: `grep -E "export (interface ProjectItem|function ProjectCard)" apps/web/src/components/projects/ProjectCard.tsx`
3. Verify client component: `grep "use client" apps/web/src/components/projects/ProjectCard.tsx`
4. Verify file length: `wc -l apps/web/src/components/projects/ProjectCard.tsx` should show >50 lines
</verification>

<success_criteria>
- ProjectCard component and ProjectItem type both exported
- Component handles variant prop correctly
- Component supports both internal (detailsSlug) and external (href) links
- Projects page can import and use ProjectCard without errors
- File passes TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix-pages/02-02-SUMMARY.md`
</output>
