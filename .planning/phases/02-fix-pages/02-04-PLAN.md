---
phase: 02-fix-pages
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Next.js build completes without module export errors"
    - "All 5 target pages compile successfully to static HTML"
    - "No new build errors introduced by component implementations"
  artifacts:
    - path: "apps/web/.next"
      provides: "Next.js build output"
      contains: "static HTML export"
    - path: "apps/web/out"
      provides: "Static export output directory"
      contains: "projects/index.html"
  key_links:
    - from: "All pages"
      to: "Implemented components"
      via: "Static build compilation"
      pattern: "next build.*output.*export"
---

<objective>
Verify all component implementations resolve build errors and pages compile successfully.

Purpose: After implementing all 4 critical components, verify the Next.js build succeeds with static export and all 5 target pages produce valid HTML. This confirms Phase 2 success criteria before proceeding to Phase 3 validation.

Output: Clean build output with no module errors, confirmation that all pages compile, and identification of any remaining issues.
</objective>

<execution_context>
@C:\Users\Daniel\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Daniel\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-pages/02-RESEARCH.md
@.planning/phases/02-fix-pages/02-01-SUMMARY.md
@.planning/phases/02-fix-pages/02-02-SUMMARY.md
@.planning/phases/02-fix-pages/02-03-SUMMARY.md

Plans 01-03 implemented all 4 critical components. This plan verifies the fixes resolve the build errors identified in Phase 1.
</context>

<tasks>

<task type="auto">
  <name>Run Next.js build and capture results</name>
  <files>apps/web/build_verification.txt</files>
  <action>
Run Next.js build to verify all component implementations work correctly.

Steps:
1. Clean previous build artifacts to ensure fresh build:
   ```bash
   cd apps/web
   rm -rf .next out build_output.txt
   ```

2. Run production build with static export:
   ```bash
   npm run build > build_verification.txt 2>&1
   ```

3. Check build exit code and capture result

4. If build succeeds:
   - Verify static export output exists: `ls out/projects/index.html out/projects/fsbo/index.html out/halborn/index.html out/softstack/index.html`
   - Capture list of generated HTML files
   - Count total pages exported

5. If build fails:
   - Parse error output to identify remaining issues
   - Categorize errors (module errors, type errors, runtime errors)
   - Identify which pages are still blocked

6. Generate summary of build results with:
   - Build success/failure status
   - List of successfully compiled pages
   - List of any remaining errors
   - Comparison to Phase 1 diagnostics (4 module errors → 0 expected)
  </action>
  <verify>
Build verification file exists: `ls apps/web/build_verification.txt`
  </verify>
  <done>
- Build executed successfully or with documented errors
- Build results captured to build_verification.txt
- If successful: static HTML files exist in out/ directory
- If failed: remaining errors are categorized and understood
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All 4 critical components implemented:
- parseAddress (address-parser.ts)
- ProjectCard (projects/ProjectCard.tsx)
- TargetSection (shared/TargetSection.tsx with halborn/softstack re-exports)

Next.js build executed to verify fixes.
  </what-built>
  <how-to-verify>
1. Review build results in `apps/web/build_verification.txt`

2. If build succeeded:
   - Check that all 5 target pages compiled:
     - /projects
     - /projects/fsbo
     - /projects/mobi (should work, no blockers per research)
     - /projects/portfolio (should work, no blockers per research)
     - /technical (ApiHealth and Sections already exist)
   - Verify static HTML exists: `ls apps/web/out/projects/index.html`
   - Compare error count: Phase 1 had 4 module errors, should now be 0

3. If build failed:
   - Review remaining errors
   - Check if they are:
     - New errors from component implementations (implementation bugs)
     - Pre-existing errors from untested pages (mobi, portfolio, technical)
     - Different module errors (unexpected blockers)
   - Determine if errors require immediate fix or can be addressed in Phase 3

4. Review implementation quality:
   - Check that components follow established patterns
   - Verify TypeScript types are correct
   - Confirm no missing dependencies

Expected outcome: Build succeeds with 0 module errors. All 5 pages compile to static HTML. Ready for Phase 3 quality validation.

Possible issues:
- Build reveals new errors in mobi, portfolio, or technical pages (research said "no blockers detected" but not build-verified)
- Type errors in component implementations
- Missing Tailwind classes or CSS variables
- Framer Motion or React Flow integration issues
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - Build succeeded, all pages compiled, ready for Phase 3
- "build-failed: [description]" - Build failed with new errors, describe what broke
- "partial: [description]" - Some pages work, others have issues, describe status
  </resume-signal>
</task>

</tasks>

<verification>
1. Check build output file exists: `ls apps/web/build_verification.txt`
2. If build succeeded:
   - Verify out/ directory: `ls apps/web/out`
   - Check target pages exist: `ls apps/web/out/projects/index.html apps/web/out/projects/fsbo/index.html`
   - Compare to Phase 1: 4 module errors should be resolved
3. If build failed:
   - Review error types and count
   - Identify if errors are from new component implementations or other sources
</verification>

<success_criteria>
- Build executes without crashing
- Module export errors from Phase 1 are resolved (4 → 0)
- Build results are documented and analyzed
- Path forward is clear (either Phase 3 or additional fixes needed)
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix-pages/02-04-SUMMARY.md`
</output>
