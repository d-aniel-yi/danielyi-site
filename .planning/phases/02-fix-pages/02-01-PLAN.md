---
phase: 02-fix-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/address-parser.ts
autonomous: true

must_haves:
  truths:
    - "parseAddress function accepts Google Maps address components and returns normalized object"
    - "FSBO page imports parseAddress without module errors"
  artifacts:
    - path: "apps/web/src/lib/address-parser.ts"
      provides: "Address parsing utility for Google Maps data"
      exports: ["parseAddress"]
      min_lines: 20
  key_links:
    - from: "apps/web/src/app/projects/fsbo/page.tsx"
      to: "apps/web/src/lib/address-parser.ts"
      via: "import statement"
      pattern: "import.*parseAddress.*from.*address-parser"
---

<objective>
Implement address parser utility to unblock FSBO deep dive page.

Purpose: The FSBO page imports parseAddress to normalize Google Maps place data for storage. The empty stub is causing module export errors that block the build.

Output: Working parseAddress function with TypeScript types that accepts Google Maps API addressComponents array and formattedAddress string, returning a normalized key-value object.
</objective>

<execution_context>
@C:\Users\Daniel\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Daniel\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fix-pages/02-RESEARCH.md

Phase 1 diagnostics identified this as one of 4 critical empty files blocking the build.
</context>

<tasks>

<task type="auto">
  <name>Implement parseAddress function</name>
  <files>apps/web/src/lib/address-parser.ts</files>
  <action>
Create parseAddress function following the signature documented in research (lines 146-152):

```typescript
export function parseAddress(
  addressComponents: google.maps.GeocoderAddressComponent[],
  formattedAddress: string
): Record<string, string>
```

Implementation requirements:
1. Define AddressComponent interface matching Google Maps structure (long_name, short_name, types[])
2. Iterate over addressComponents array
3. Map Google Maps types to stable keys:
   - street_number → street_number
   - route → street / street_name
   - locality → city
   - administrative_area_level_1 → state
   - postal_code → zip
   - country → country
4. Return normalized Record<string, string> object
5. Handle missing components gracefully (don't crash if component missing)
6. Add formattedAddress to returned object as fallback

This is a stub implementation to unblock the build. Per decision diag-003, use basic extraction logic initially.

Use TypeScript strict mode. No external dependencies needed.
  </action>
  <verify>
1. File is non-zero size
2. TypeScript compiles without errors: `cd apps/web && npx tsc --noEmit src/lib/address-parser.ts`
3. Export exists and has correct signature
  </verify>
  <done>
- parseAddress function is exported
- Function accepts addressComponents array and formattedAddress string
- Function returns Record<string, string>
- TypeScript types are defined
- No compilation errors
  </done>
</task>

</tasks>

<verification>
1. Run `cd apps/web && npx tsc --noEmit src/lib/address-parser.ts` - should pass
2. Verify exports: `grep -E "export.*parseAddress" apps/web/src/lib/address-parser.ts`
3. Verify file is non-empty: `wc -l apps/web/src/lib/address-parser.ts` should show >20 lines
</verification>

<success_criteria>
- parseAddress function implemented with correct TypeScript signature
- Function handles Google Maps addressComponents structure
- FSBO page can import parseAddress without module errors
- File passes TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/phases/02-fix-pages/02-01-SUMMARY.md`
</output>
